<%= link_to 'Back', posts_path, class: 'btn btn-secondary mb-3' %>
<div class='row'>
  <div class=>
    <div class='card'>
      <div class='card'>
        <div class='card-body'>
          <h4 class='card-titlt'>Editing Post</h4>
          <%= render 'form', post: @post %>
        </div>
      </div>
    </div>
  </div>
</div>

<%= link_to 'Show', @post %>
<%= button_to 'Destroy', @post,
            method: :delete,
            data: { confirm: 'Are you sure?' }, class:'btn btn-primary' %>

