<%= form_with(model: [ @post, @post.suggestions.build ]) do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body, required: true %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
